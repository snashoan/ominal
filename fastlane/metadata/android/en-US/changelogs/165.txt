Persistent per-chat agent sessions keep context alive, queue same-chat turns while other chats run in parallel, and let long-press Send reopen the chat harness terminal. The hidden native display now prewarms reliably, verifies X readiness before computer use, reports an explicit idle state, highlights the screen edge while an agent is controlling it, and returns to chat reliably after switching chats.
