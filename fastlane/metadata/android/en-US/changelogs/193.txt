Each chat now keeps an isolated, persistent workspace and resumable harness context. Activity shows verified commands, paths, file work, and outputs without exposing private reasoning. Code and math rendering are clearer, registered harnesses can declare interactive terminals, Codex model discovery falls back to its own cache, runtime restarts skip healthy setup, and launcher icons are consistent across Android themes.
